spring:
  datasource:
    url: jdbc:mysql://localhost:3306/EMPLOYEE_DB?useSSL=false&serverTimezone=UTC
    username: root
    password: 11032003
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: very-secret-key-very-secret-key-very-secret-key-123456
  expiration: 86400000
  issuer: employee-app
